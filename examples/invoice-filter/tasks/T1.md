# T1: implement invoice status filtering

Synthetic Work Order fixture only. In a real repository, Astra supplies the exact
Invoice type, workspace, baseline, and current architecture.

Goal: implement `filter_invoices(invoices, status=None)` so it returns a new list,
preserves order and record identity, accepts `open`, `paid`, `void`, or `None`, and
raises `ValueError` for any other status. Do not mutate the input.

Allowed paths: `src/invoices/filtering.py` and
`tests/test_invoice_filtering.py`. No payment/provider calls, dependencies,
production data, unrelated refactor, or API changes.

Test no filter, every valid status, empty input, invalid status, order, identity,
and non-mutation. Run the named test command, fix in-scope failures, then return the
actual changed paths, command exit, acceptance coverage, and remaining risks.
