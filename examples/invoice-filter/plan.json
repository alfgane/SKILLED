{
  "schema_version": 2,
  "project": "Synthetic invoice status filter example",
  "spec": "spec.md",
  "phases": [
    {
      "id": "P1",
      "goal": "A pure status filter with order preservation and input validation",
      "depends_on": [],
      "integration_checks": [
        {"command": "python -m unittest discover -s tests", "expected": "Application tests pass"}
      ]
    }
  ],
  "tasks": [
    {
      "id": "T1",
      "phase": "P1",
      "executor": "sol",
      "risk": "normal",
      "state": "planned",
      "depends_on": [],
      "brief": "tasks/T1.md",
      "allowed_paths": ["src/invoices/filtering.py", "tests/test_invoice_filtering.py"],
      "acceptance": [
        "Matches the chosen status while preserving order and record identity",
        "Invalid status raises ValueError",
        "Input collection is not mutated"
      ],
      "checks": [
        {"command": "python -m unittest discover -s tests -p test_invoice_filtering.py", "expected": "Filter behavior tests pass"}
      ]
    }
  ]
}
